export enum Operator {
    ADD = 'add',
    SUBTRACT = 'subtract',
    MULTIPLY = 'multiply',
    DIVIDE = 'divide'
}

export enum ConvertOperator {
    add =  '+',
    subtract = '-',
    multiply = '*',
    divide = '/'
}